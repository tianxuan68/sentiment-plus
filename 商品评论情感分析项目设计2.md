# 基于深度学习与自然语言处理的商品评论情感分析系统

# 一、项目概述

项目的背景是现在电商平台每天都会产生大量用户评论，这些评论里面包含了用户对商品质量、价格、物流、服务等方面的真实反馈。但是由于评论数量非常大，人工很难快速分析这些信息，所以我们希望通过 NLP 技术自动挖掘评论中的有效信息。

这个项目主要解决三个问题。第一个是**情感分析**，通过机器学习和深度学习模型判断用户评论是正向还是负向，帮助了解用户整体满意度。第二个是**关键词提取**，从大量评论中找到用户关注的重点，比如商品的优点和缺点。第三个是**属性级情感分析**，比如用户评价一件商品时，我们不仅判断整体情感，还可以分析他对质量、价格、物流等不同方面的态度。

主要功能：

-   商品评论情感分类
-   用户评价分析
-   商品优缺点挖掘
-   评论关键词提取
-   商品属性情感分析

> **分工说明（当前版）**：人员以实名 + 接口编号并用。情感对比链为 **Baseline → BiLSTM → BERT**（三模型，不含 CNN）。**6 号**专职路演 PPT 汇总制作；陈江平需保证 BiLSTM 相对 Baseline F1 提升 ≥2%。满意度统计、BERT 属性级情感两岗人员待补全时，接口仍按「交 1 号」约定对接。

------------------------------------------------------------------------

# 二、技术路线

整体流程：

    商品评论数据
            ↓
    数据预处理（清洗 / 分词）
            ↓
    文本向量表示（TF-IDF / Embedding / BERT）
            ↓
    机器学习模型 / 深度学习模型
            ↓
    NLP高级分析（关键词 / 优缺点 / 属性）
            ↓
    系统展示（FastAPI + Vue + ECharts）

------------------------------------------------------------------------

# 三、团队分工

| 功能模块 | 人员 | 负责任务 | 使用技术 | 验收标准 |
| -------- | ---- | -------- | -------- | -------- |
| **商品评论情感分类** | 毛鑫泽 | 传统 ML 情感分类；正负预测；建 Baseline | TF-IDF、KNN、Decision Tree、Random Forest、Scikit-learn | ①≥3 种传统模型；②测试集 Acc≥85%；③输出 Acc/P/R/F1；④导出最佳 Baseline |
| | 陈江平 | LSTM/BiLSTM；上下文语义建模 | PyTorch、Embedding、BiLSTM、Attention | ①双向语义建模；②F1≥0.88；③相对 CNN 的 F1 提升≥2%；④完成 Baseline/CNN/BiLSTM 三方对比 |
| | 胡潇潇 | BERT 情感分类微调 | PyTorch、Transformer、bert-base-chinese | ①完成 Fine-tuning；②Acc≥92%；③F1≥0.90；④优于 CNN/BiLSTM；⑤向 1 号提供推理入口 |
| **PPT 制作** | 6号 | 路演 PPT 制作与汇总 | PPT / 素材归档 | ①汇总全员路演素材；②完成融资/答辩 PPT |
| **用户评价分析** | 郑平高 | 评价分析系统开发与可视化 | FastAPI、Vue、ECharts | ①接入情感/关键词/属性接口；②评论输入与结果展示；③比例图+趋势图；④响应≤2 秒 |
| | 杨国东 | 数据采集与数据集整理 | 公开数据集、Pandas | ①英文≥5 万条；②字段统一；③输出 `unified_reviews.csv`；④抽≥3000 句交 5 号 |
| | 待定 | 评价统计分析；满意度指标 | Pandas、Numpy | ①正负比例；②评分分布；③满意度报告交 1 号 |
| **商品优缺点挖掘** | 邓新晓 | 优缺点自动挖掘 | KeyBERT、Sentence-BERT、MMR | ①`pros_top10.csv`；②`cons_top10.csv`；③人工相关率≥80%；④接口交 1 号 |
| **评论关键词提取** | 刘攀 | 评论文本预处理 | re、jieba、stopwords | ①清洗；②分词去停用词≥95%；③导出 `clean_train.csv` |
| | 杨国东 | 关键词提取算法 | KeyBERT；TF-IDF 基线 | ①Top-N；②准确率≥80%；③写清相对 TF-IDF 提升；④接口交 1 号 |
| **商品属性情感分析** | 5号 | 属性实体识别与标注 | BertForTokenClassification、BIO | ①BIO+polarity 双标注；②实体 F1≥85%；③样本交 7/8 号 |
| | 陈江平 | 属性与情感关系建模 | BiLSTM、Attention | ①统一划分训练测试；②F1≥85%；③导出 `bilstm_aspect_best.pt` |
| | 待定 | BERT 属性级情感分类 | bert-base-chinese、句对输入 | ①Acc≥90%；②一句多属性预测；③权重交 1 号 |

### 3.1 按人员速览

| 人员 | 编号/角色 | 主责 | 说明 |
| ---- | --------- | ---- | ---- |
| 郑平高 | 前后端 | 前后端系统 | FastAPI / Vue / ECharts；汇总 Demo 与路演系统素材 |
| 杨国东 | 数据+关键词 | 数据采集统一表；KeyBERT 关键词 | 抽 ≥3000 句交 **5 号**；关键词接口交 **1 号** |
| 刘攀 | 预处理 | 文本清洗与分词 | 产出 `clean_train.csv`（含 `text_clean`）供下游 |
| 毛鑫泽 | Baseline | 传统 ML 情感 Baseline | TF-IDF + ≥3 种分类器；路演对比柱状图 |
| 5号 | NER | 属性实体识别与双标注 | `aspect_ner.jsonl`、`aspect_sentiment.csv` |
| 6号 | PPT | 路演 PPT 制作 | 汇总各员 `pitch_assets/` 素材成片 |
| 陈江平 | 深度学习（序列） | BiLSTM 情感 + 属性情感 | 对照 CNN「+2% F1」；属性权重 `bilstm_aspect_best.pt` |
| 胡潇潇 | 深度学习（预训练） | BERT 情感微调与主推理 | 向 1 号提供推理入口；四模型对比素材 |
| 邓新晓 | 优缺点 | KeyBERT 优/缺点挖掘 | `pros_top10` / `cons_top10` 交 1 号 |
| 待定 | 统计 | 满意度指标 | 饼图 / 评分分布 / KPI 卡交 1 号 |
| 待定 | 属性 BERT | BERT 属性级情感 | 多属性面板素材交 1 号 |

------------------------------------------------------------------------

# 四、技术点标注

|                  | **BERT-NER**                                                 | **KeyBERT**                                                  |
| :--------------- | :----------------------------------------------------------- | :----------------------------------------------------------- |
| **大白话概念  ** | "文本中的专用名词标注" 一段话里的"人名"、"地名"、"机构名"、"时间"等特定词汇并打标签。 | "关键词提取" 将整段话和候选词都转成语义向量，相似度最高的即为关键词。 |
| **工作流程 **    | **① Token 化**：`BertTokenizer` 拆分为子词，加 `[CLS]` / `[SEP]`。<br /> **② 特征提取**：双向 Transformer 编码器提取上下文语义向量。 <br />**③ 序列标注**：每个 Token 传入线性层，预测 BIO 标签（用来标记文本中每个 token 属于实体的哪个位置）。 <br />**④ 实体抽取**：按标签连字成词，拼出完整实体。 | **① 候选生成**：切分为 n-gram 候选词/词组。 <br />**② 语义编码**：Sentence-BERT 分别计算**文档向量**和**候选词向量**。 <br />**③ 相似度排序**：计算文档与候选词的余弦相似度。 <br />**④ 多样性过滤**：MMR / MSS 剔除语义重复词，选出 Top-K。 |
| **常用 API**     | `transformers`<br />.`BertForTokenClassification` *(BERT 底座 + Token 分类头)* <br />可选接 `TorchCRF` 做 BIO 标签序列约束 | `keybert.KeyBERT` *(封装 **SentenceTransformer 的主类**)* `.extract_keywords(docs, ...)` — 提取关键词核心方法 |
| **常用参数**     | `pretrained_model_name_or_path`: 预训练权重<br />`num_labels`: 标签类别数<br />`input_ids`: 词向量 ID 序列 `[B, L]` <br />`hidden_size`: 隐层维度（默认 768） `classifier_dropout`: 分类头 Dropout | `model`: **SentenceTransformer 模型** `docs`: 输入文本 `keyphrase_ngram_range`: 词组长度范围，如 `(1, 2)` <br />`stop_words`: 停用词（`'english'`） `top_n`: 返回前 N 个关键词 <br />`use_mmr`: 是否开启最大边际相关性 `diversity`: MMR 多样性因子 `0.0~1.0` `nr_candidates`: MMR 候选池大小 |
| 参数解释1        | `classifier_dropout`：<br />分类层在训练时**随机**关闭一部分神经元的比例 | `use_mmr`： <br />**默认关闭时**，**KeyBERT**只看**这个词和整篇文章有多像**，按照相似度从高到低排序，取出来前**nr_candidates**个。**选出意思相近的词**            <br />**开启后**，KeyBERT 会额外考虑**这个词和已经选出的词有多像**，**尽量避开重复意思，让结果覆盖面更广**。 |
| 参数解释2        |                                                              | `diversity`： **<br />小（接近 0）**：只看和文章像不像，结果可能重复  <br />**值大接近 1**：只看彼此差异大不大，结果可能偏离文章主题 <br />**中间值0.5 左右）**：两者兼顾，最常用 |
| 参数解释3        |                                                              | `nr_candidates`： <br />先把所有**候选词按与文章的相似度排序**，**取出前 `nr_candidates` 个**组成一个子集，然后只在这个**子集内部用计算多样性和相关性**，挑出最终关键词。 |
| 参数解释4        |                                                              | `SentenceTransformer`： <br />模型分别计算**整篇文档的向量**和**每个候选词/短语的向量**，然后通过余弦相似度排序，找出与文档语义最接近的关键词 |

------------------------------------------------------------------------

# 五、工作细节

## 5.1 数据与规则

| 项 | 约定 |
| -- | ---- |
| 中文主数据 | `train.csv`：`sentence`（评论）、`label`（0负/1正）、`dataset`；约 4.5 万条 |
| 英文辅数据 | `data.csv`：`reviewText`、`overall`（0～4）、`asin`、`reviewTime` / `unixReviewTime` 等；≥5 万条 |
| 划分规则 | train:val:test = 8:1:1，`random_seed=68`，全组共用同一测试集；中英不混训 |
| 清洗语料 | 刘攀产出 `clean_train.csv`（`sentence_raw`、`tokens`、`text_clean`、`label`） |
| 属性样本 | 杨国东抽样 → **5 号** BIO + polarity 双标注 → `aspect_sentiment.csv` 供 **陈江平 / 属性 BERT** |
| 情感对比链 | Baseline（毛鑫泽）→ TextCNN → BiLSTM（陈江平）→ BERT（胡潇潇）；**6 号**汇总 PPT |

## 5.2 分工工作细节

| 功能模块 | 人员 | 负责任务 | 工作细节 | 技术实现细节 |
| -------- | ---- | -------- | -------- | ------------ |
| **商品评论情感分类** | 毛鑫泽 | 传统 ML 情感分类；正负预测；建 Baseline | ①统一划分读训练/测试集<br />②仅训练集拟合 TF-IDF 与分类器<br />③调参 ≥3 种模型<br />④测试集 Acc≥85%，导出最佳 Baseline<br />⑤【路演】配合 PPT 生成模型对比柱状图（Acc/F1）+ 指标表 | **数据**：`train.csv` 的 `sentence`、`label`；用刘攀 `text_clean`<br />**技术**：`TfidfVectorizer`（`max_features=20000`，`ngram_range=(1,2)`）+ KNN / DecisionTree / RandomForest（≥3 种）<br />**评估**：Acc / P / R / F1、`classification_report` |
| **PPT 制作** | 6号 | PPT 制作 | ①收集各员 `pitch_assets/` 图文<br />②按路演大纲排版成片<br />③统一风格与指标口径 | 工具：PPT / Keynote；素材规范见 5.3 |
| **商品评论情感分类** | 陈江平 | LSTM/BiLSTM；上下文语义建模 | ①复用统一划分与共享词表<br />②BiLSTM+Attention，早停<br />③F1≥0.88，相对 CNN +≥2%<br />④三方对比报告（Baseline/CNN/BiLSTM）<br />⑤【路演】配合 PPT 生成 Baseline/CNN/BiLSTM 对比图；标「+2% F1」 | **数据**：与全组同一划分<br />**技术**：Embedding + BiLSTM + Attention + Linear<br />**对比**：`BiLSTM_F1 − CNN_F1 ≥ 0.02` |
| | 胡潇潇 | BERT 情感分类微调 | ①统一划分微调<br />②Acc≥92%、F1≥0.90<br />③优于 CNN/BiLSTM 后导出权重<br />④向 1 号（郑平高）提供推理入口<br />⑤【路演】配合 PPT 生成 SOTA 指标卡 + 四模型对比图；2～3 条样例 | **数据**：`sentence`、`label`<br />**技术**：`BertForSequenceClassification('bert-base-chinese', num_labels=2)`<br />**超参**：`max_length=128`，`lr=2e-5`，`batch_size=16`，`epochs=2~4`，AdamW |
| **前后端 / 用户评价分析** | 郑平高（1号） | 评价分析系统开发与可视化 | ①接入情感/关键词/属性接口<br />②评论输入与结果展示<br />③比例图 + 趋势图<br />④响应 ≤2 秒<br />⑤【路演】配合 PPT 生成汇总素材；UI 截图/15s GIF；Demo 页 | **技术**：FastAPI + Vue + ECharts<br />**比例图**：`label` / `pred_label`<br />**趋势图**：`unixReviewTime` / `reviewTime` |
| **用户评价分析** | 杨国东 | 数据采集与数据集整理 | ①校验规模与字段<br />②清洗空文本与编码<br />③输出统一表（不混训）<br />④抽 ≥3000 句交 5 号标注<br />⑤【路演】配合 PPT 生成数据规模一页纸 | **中文**：`train.csv` → `sentence`、`label`、`dataset`<br />**英文**：`data.csv` → `reviewText`、`overall`、`asin`（≥5 万）<br />**产出**：`unified_reviews.csv` |
| | 待定 | 评价统计分析；满意度指标 | ①正负比例<br />②评分分布<br />③满意度报告交 1 号<br />⑤【路演】配合 PPT 生成饼图、评分柱状图、KPI 卡 | **正负比例**：`label` / `pred_label`<br />**评分分布**：`overall`（0～4）<br />**工具**：Pandas / Numpy |
| **商品优缺点挖掘** | 邓新晓 | 优缺点自动挖掘 | ①`pros_top10.csv`<br />②`cons_top10.csv`<br />③人工相关率 ≥80%<br />④结果与接口交 1 号<br />⑤【路演】配合 PPT 生成词云 + Top10 对照表 | **数据**：刘攀 `text_clean`/`sentence` + `label`（正/负桶）<br />**技术**：`KeyBERT('paraphrase-multilingual-MiniLM-L12-v2')`<br />**参数**：`ngram=(1,2)`，`top_n=10`，`use_mmr=True`，`diversity=0.5`，`nr_candidates=20` |
| **评论关键词提取** | 刘攀 | 评论文本预处理 | ①清洗<br />②分词去停用词（≥95%）<br />③导出供下游<br />⑤【路演】配合 PPT 生成清洗前/后对照样例 | **输入**：`train.csv.sentence`<br />**技术**：`re` + `jieba` + `stopwords.txt`<br />**产出**：`clean_train.csv`（`sentence_raw`、`tokens`、`text_clean`、`label`） |
| | 杨国东 | 关键词提取算法 | ①Top-N 关键词<br />②准确率 ≥80%<br />③写清相对 TF-IDF 提升<br />④接口交 1 号<br />⑤【路演】配合 PPT 生成 KeyBERT vs TF-IDF 对照图 | **数据**：刘攀 `clean_train.csv`<br />**技术**：KeyBERT；基线 TF-IDF Top-N<br />**评测**：人工准确率 vs TF-IDF |
| **商品属性情感分析** | 5号 | 属性实体识别与标注 | ①BIO + polarity 双标注<br />②NER，实体 F1≥85%<br />③输出抽取结果<br />④样本交 7/8 号（陈江平 / 属性 BERT）<br />⑤【路演】配合 PPT 生成 BIO 示例、5 类覆盖图、NER F1 卡 | **语料**：杨国东抽 ≥3000 句<br />**实体**：≥5 类，BIO → `aspect_ner.jsonl`<br />**情感**：polarity → `aspect_sentiment.csv`<br />**模型**：`BertForTokenClassification` |
| | 陈江平 | 属性与情感关系建模 | ①统一划分加载<br />②训练测试<br />③F1≥85%<br />④导出 `bilstm_aspect_best.pt`<br />⑤【路演】配合 PPT 生成 F1 数字卡 + 样例 | **数据**：5 号 `aspect_sentiment.csv`<br />**技术**：BiLSTM + Attention<br />**评估**：`f1_score` |
| | 待定 | BERT 属性级情感分类 | ①Acc≥90%<br />②一句多属性预测<br />③权重交 1 号<br />⑤【路演】配合 PPT 生成多属性结果面板截图 | **数据**：5 号 `aspect_sentiment.csv`<br />**技术**：`bert-base-chinese`；`[CLS] sentence [SEP] aspect [SEP]`<br />**多属性**：一句多 aspect 推理 |

## 5.3 路演 PPT 需要的素材清单

**6 号**汇总进 PPT；**郑平高（1 号）**补充系统 Demo 素材。导出 PNG（宽 ≥1920）放到 `pitch_assets/<人员>/`。

| 提供人 | PPT 页（建议） | 需要的数据 / 图片 | 制作方式（技术员按此做） |
| ------ | -------------- | ----------------- | ------------------------ |
| 郑平高 | 封面 / 一句话价值 | 系统首页图 | 浏览器全屏截图或 Playwright 导出 PNG |
| 郑平高 | 解决方案 / 产品 | 技术路线流程图 | draw.io / PPT 框图导出 |
| 郑平高 | 产品 Demo | 操作演示动图 | 录屏后转 10～15 秒 GIF/MP4 |
| 6号 | 成片汇总 | 全员素材合订 | 按本节清单排版；统一字体与指标口径 |
| 杨国东 | 痛点 / 市场 / 数据资产 | 中英规模对比、字段说明 | Pandas 统计 + `plt.bar`；字段表/图 |
| 刘攀 | 数据处理能力 | 清洗前/后对照 | 2～3 条样例表图 |
| 毛鑫泽 | 核心效果 | Baseline 多模型 Acc/F1 柱状图 + 指标表 | KNN/DT/RF 指标 → 分组柱状图 |
| 待定（统计） | 数据资产 / 商业闭环 | 正负饼图、评分柱状图、KPI 卡 | `label` / `overall` → pie/bar + 数字卡 |
| 5号 | 属性级洞察 | BIO 示例、5 类覆盖图、NER F1 卡 | 标注着色图 + `plt.bar` + 指标卡 |
| 陈江平 | 核心效果 / 属性 | Baseline/CNN/BiLSTM 对比图（标 +2% F1）；属性 F1 卡 | 分组柱状图 + 数字卡 + 样例 |
| 胡潇潇 | 核心效果 / 属性 | SOTA 指标卡、四模型对比图、2～3 条样例；多属性面板（若兼属性 BERT） | 大字指标卡 + 对比柱状图 + 样例表 |
| 邓新晓 | 关键词 / 优缺点 | 优/缺点词云、Top10 对照表 | WordCloud + 对照表 |
| 杨国东 | 关键词 | KeyBERT vs TF-IDF 对照图 | 水平柱状/并列表 |

------------------------------------------------------------------------

# 六、仓库与目录对应（摘要）

详见根目录 `README.md`。AI 侧按功能分包，便于按人并行：

| 目录（`sentiment-ai/`） | 主要负责人 |
| ----------------------- | ---------- |
| `data/` | 杨国东 |
| `pipelines/preprocess/` | 刘攀 |
| `pipelines/sentiment/baseline/` | 毛鑫泽 |
| `pipelines/sentiment/cnn/` | 对比用 TextCNN（随对比实验落地） |
| `pipelines/sentiment/bilstm/` | 陈江平 |
| `pipelines/sentiment/bert/` | 胡潇潇 |
| `pipelines/keywords/` | 杨国东 |
| `pipelines/pros_cons/` | 邓新晓 |
| `pipelines/aspect_ner/` | 5号 |
| `pipelines/aspect_sentiment/` | 陈江平 + 属性 BERT（待定） |
| `analytics/` | 满意度统计（待定） |
| `app/`（推理 API） | 对接 郑平高；主推理由胡潇潇提供入口 |
| `pitch_assets/` | 6号汇总；各员自建子目录交图 |
| `jeecg-fastapi/` + `jeecgboot-vue3/` | 郑平高 |
