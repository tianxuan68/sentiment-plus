# 商品评论情感分析 ↔ szai6_tmf_project 对照（人员版）

> 来源：[`商品评论情感分析项目设计2.md`](./商品评论情感分析项目设计2.md)  
> 目的：只回答一件事——**谁相当于在做 szai6 哪一段**。  
> 说明：本项目是「评论情感 + 属性/关键词」；szai6 是「新闻分类全链路」。下面按 **szai6 写代码的阶段顺序** 对照，不改你们原有分工。

------------------------------------------------------------------------

## 一、szai6 阶段速览（写代码顺序）

```
00_test          冒烟（Flask / FastAPI / Dataset）
_01_data         数据与划分、停用词、Config
_02_rf           TF-IDF + 传统 ML（随机森林等）
_03_fasttext     FastText
_04_bert_base    BERT 微调 + predict_fun + Web 部署
_05_llm_prompt   LLM 提示词分类
_06 ~ _08        量化 / 蒸馏 / 剪枝
```

每阶段内部习惯顺序：`Config → 数据 → 模型 → 训练/评估 → predict_fun → Flask`。

------------------------------------------------------------------------

## 二、人员 ↔ szai6 阶段（核心表）

| szai6 阶段 | 本项目相当于谁 | 本项目在做什么 | 备注 |
| ---------- | -------------- | -------------- | ---- |
| **`_01_data`** | **杨国东**（主）+ **刘攀**（预处理） | 数据采集 / `unified_reviews.csv` / 划分约定；清洗分词、`clean_train.csv`、停用词 | 杨国东≈原始数据与统一表；刘攀≈停用词+清洗（szai6 里常跟 data 绑在一起） |
| **`_02_rf`** | **毛鑫泽** | TF-IDF + KNN / 决策树 / 随机森林 Baseline | 最像 `_02_rf`：传统 ML + 向量特征 |
| **`_03_fasttext`** | （无专人） | — | 本设计未设 FastText 岗；对比链是 Baseline → BiLSTM → BERT |
| **（szai6 无独立阶段，夹在 rf 与 bert 之间）** | **陈江平** | BiLSTM 整句情感 + 属性情感 | 相当于「深度序列模型」这一环，写在 `_02` 之后、`_04` 之前 |
| **`_04_bert_base`（训练 + predict_fun）** | **胡潇潇** | BERT 情感微调、主推理入口、`predict_fun` / 推理 API | 对应 `_04` 里「模型 + 训练 + predict_fun」 |
| **`_04_bert_base`（Web 部署 / 展示）** | **郑平高（1号）** | FastAPI + Vue + ECharts 业务系统；聚合各接口 | 对应 `_04` 里「基于 Flask/FastAPI 的 Web 部署」；本项目拆成业务后端+前端 |
| **`00_test`** | **郑平高** + **胡潇潇**（轻量） | 联调冒烟、健康检查、Demo | 联调时才像 `00_test`，不是专职阶段 |
| **`_05_llm_prompt`** | （无） | — | 本设计未做 LLM 提示词分类 |
| **`_06`～`_08` 压缩** | （无） | — | 无量化 / 蒸馏 / 剪枝分工 |

------------------------------------------------------------------------

## 三、本项目多出来的部分（szai6 新闻分类没有）

这些**不是** szai6 标准阶段，但在本项目里很重要：

| 人员 | 本项目职责 | 若硬套 szai6，大概挂在哪 |
| ---- | ---------- | ------------------------ |
| **5号** | 属性 NER（BIO + polarity） | 挂在 `_04` 之后的「进阶 NLP」；不对应 `_01`～`_08` 任一标准目录 |
| **杨国东**（关键词） | KeyBERT / vs TF-IDF | 偏应用层 NLP，成果交 1 号展示 |
| **邓新晓** | 优缺点挖掘（pros/cons） | 同上，交 1 号展示 |
| **待定（统计）** | 满意度 / 评分分布 | 偏看板指标，交 1 号 |
| **待定（属性 BERT）** | BERT 属性级情感 | 可看成 `_04` 的第二套任务（句对/多属性） |
| **6号** | 路演 PPT 汇总 | 不在 szai6 代码阶段里 |

------------------------------------------------------------------------

## 四、一句话记忆

| 想对齐 szai6 时 | 找谁 |
| --------------- | ---- |
| 数据怎么来的 | 杨国东 → 刘攀 |
| 传统 ML Baseline | 毛鑫泽（≈`_02_rf`） |
| 深度序列（BiLSTM） | 陈江平 |
| BERT + 推理 | 胡潇潇（≈`_04` 模型侧） |
| 网页 / 系统展示 | 郑平高（≈`_04` 部署侧） |
| 属性实体 | 5号（szai6 没有的扩展） |
| 关键词 / 优缺点 | 杨国东 / 邓新晓（扩展） |
| PPT | 6号 |

```
szai6 主链：   _01_data → _02_rf → (_03_fasttext 本项目无) → _04_bert → Web
本项目主链：   杨国东/刘攀 → 毛鑫泽 → 陈江平(BiLSTM) → 胡潇潇 → 郑平高
扩展支线：     5号(NER) · 杨国东(关键词) · 邓新晓(优缺点) · 待定(统计/属性BERT) → 都交 1 号
路演：         全员 pitch_assets → 6号
```
