# 待定 · BERT 属性级情感分类 · 任务细化

> 对齐主设计：商品属性情感分析 · BERT 句对输入；权重交 1 号  
> 人员补全后：改文件名/子目录为人名即可，接口约定不变。  
> 全局约定见：[00-总览与全局约定.md](./00-总览与全局约定.md)

------------------------------------------------------------------------

# 一、岗位速览

| 项 | 内容 |
| -- | ---- |
| **人员** | 待定 |
| **主责** | BERT 属性级情感 |
| **目录** | `pipelines/aspect_sentiment/bert/` |
| **使用技术** | `bert-base-chinese`、句对输入 |
| **验收标准** | ①Acc≥90%；②一句多属性预测；③权重交 1 号 |

------------------------------------------------------------------------

# 二、输入

- 5 号 `data/processed/aspect_sentiment.csv`

------------------------------------------------------------------------

# 三、实现步骤

1. 输入格式：`[CLS] sentence [SEP] aspect [SEP]`。
2. 微调二分类/三分类（与 `polarity` 标签一致）；测试 Acc≥90%。
3. 推理支持**一句多 aspect**：循环或 batch 多个 aspect，返回列表。
4. 权重目录：`artifacts/aspect/bert_aspect_best/`；**交 1 号**挂多属性面板。
5. 可选在 `app` 暴露：

| 方法 | 路径 | 请求 | 响应 |
| ---- | ---- | ---- | ---- |
| POST | `/api/v1/aspect/predict` | `{ "text": "...", "aspects": ["质量","价格"] }` | `{ "items": [{"aspect":"质量","label":1,"prob":0.93}] }` |

6. 【路演】多属性结果面板截图（人员确定后放入对应 `pitch_assets/`）。

------------------------------------------------------------------------

# 四、产出文件

| 路径 | 说明 |
| ---- | ---- |
| `artifacts/aspect/bert_aspect_best/` | 权重 |
| 多属性面板截图 | 交 6 号 / 1 号 |

------------------------------------------------------------------------

# 五、对接

| 交谁 | 交什么 |
| ---- | ------ |
| **郑平高（1号）** | 权重路径或推理 URL |
| 6号 | 面板截图 |

------------------------------------------------------------------------

# 六、交付自检

| 检查项 | 达标 |
| ------ | ---- |
| Acc≥90% | ☐ |
| 一句多属性可预测 | ☐ |
| 权重/接口交 1 号 | ☐ |
| 路演面板截图已交 | ☐ |
