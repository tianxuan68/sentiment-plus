# 邓新晓 · 优缺点自动挖掘 · 任务细化

> 对齐主设计：商品优缺点挖掘 · KeyBERT + MMR  
> 全局约定见：[00-总览与全局约定.md](./00-总览与全局约定.md)

------------------------------------------------------------------------

# 一、岗位速览

| 项 | 内容 |
| -- | ---- |
| **人员** | 邓新晓 |
| **主责** | KeyBERT 优/缺点挖掘 |
| **目录** | `sentiment-ai/pipelines/pros_cons/` |
| **使用技术** | KeyBERT、Sentence-BERT、MMR |
| **验收标准** | ①`pros_top10.csv`；②`cons_top10.csv`；③人工相关率≥80%；④接口交 1 号 |
| **路演目录** | `sentiment-ai/pitch_assets/deng_xinxiao/` |

------------------------------------------------------------------------

# 二、输入

- 刘攀 `clean_train.csv`：`text_clean`/`sentence` + `label`（正/负分桶）

------------------------------------------------------------------------

# 三、实现步骤

1. 正样本桶（`label=1`）跑 KeyBERT → Top10 优点；负样本桶（`label=0`）→ Top10 缺点。
2. 模型与参数（对齐主设计）：
   - `KeyBERT('paraphrase-multilingual-MiniLM-L12-v2')`
   - `keyphrase_ngram_range=(1, 2)`
   - `top_n=10`
   - `use_mmr=True`
   - `diversity=0.5`
   - `nr_candidates=20`
3. 导出：
   - `data/processed/pros_top10.csv`（列建议：`rank`、`phrase`、`score`）
   - `data/processed/cons_top10.csv`
4. 人工评测相关率 ≥80%（抽样打分表可放 `artifacts/metrics/pros_cons_human_eval.csv`）。
5. 可选推理接口交 1 号：`POST /api/v1/pros_cons/extract`，或直接提供静态 Top10 供前端展示。
6. 【路演】词云 + Top10 对照表 → `pitch_assets/deng_xinxiao/`。

------------------------------------------------------------------------

# 四、产出文件

| 路径 | 说明 |
| ---- | ---- |
| `data/processed/pros_top10.csv` | 优点 Top10 |
| `data/processed/cons_top10.csv` | 缺点 Top10 |
| `artifacts/metrics/pros_cons_human_eval.csv` | 人工相关率 |
| `pitch_assets/deng_xinxiao/pros_cons_cloud.png` | 词云 |
| `pitch_assets/deng_xinxiao/top10_table.png` | 对照表 |

------------------------------------------------------------------------

# 五、对接

| 交谁 | 交什么 |
| ---- | ------ |
| **郑平高（1号）** | CSV 或 HTTP 接口 |
| 6号 | 词云与对照表 |

------------------------------------------------------------------------

# 六、交付自检

| 检查项 | 达标 |
| ------ | ---- |
| `pros_top10.csv` / `cons_top10.csv` 已出 | ☐ |
| 人工相关率 ≥80% | ☐ |
| 结果/接口交 1 号 | ☐ |
| 路演：词云 + Top10 表 | ☐ |
