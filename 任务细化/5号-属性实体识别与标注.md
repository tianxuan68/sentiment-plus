# 5号 · 属性实体识别与标注 · 任务细化

> 对齐主设计：商品属性情感分析 · BIO + polarity 双标注；NER  
> 全局约定见：[00-总览与全局约定.md](./00-总览与全局约定.md)

------------------------------------------------------------------------

# 一、岗位速览

| 项 | 内容 |
| -- | ---- |
| **人员** | 5号 |
| **主责** | 属性实体识别与双标注 |
| **目录** | `sentiment-ai/pipelines/aspect_ner/` |
| **使用技术** | `BertForTokenClassification`、BIO |
| **验收标准** | ①BIO+polarity 双标注；②实体 F1≥85%；③样本交 7/8 号（陈江平 / 属性 BERT） |
| **路演目录** | `sentiment-ai/pitch_assets/no05_ner/` |

------------------------------------------------------------------------

# 二、输入

- 杨国东 `data/samples/aspect_annotate_pool.csv`（≥3000 句）

------------------------------------------------------------------------

# 三、实现步骤

1. 定义 **≥5 类** 属性实体（示例口径，组内可改名但须固定）：质量、价格、物流、服务、包装（或同级五类）。
2. **双标注**：
   - 序列：BIO → `data/processed/aspect_ner.jsonl`  
     每行示例：`{"id": "...", "tokens": [...], "labels": ["O","B-质量","I-质量",...]}`
   - 方面情感：`data/processed/aspect_sentiment.csv`  
     列：`sentence`、`aspect`、`polarity`（及可选 `aspect_span`）
3. 训练 `BertForTokenClassification`；实体级 F1 **≥85%**；指标写入 `artifacts/metrics/ner_f1.json`。
4. 将 `aspect_sentiment.csv`（及划分说明）交 **陈江平** 与 **属性 BERT（待定）**。
5. 【路演】BIO 着色示例、5 类覆盖图、NER F1 卡 → `pitch_assets/no05_ner/`。

------------------------------------------------------------------------

# 四、产出文件

| 路径 | 说明 |
| ---- | ---- |
| `data/processed/aspect_ner.jsonl` | BIO 标注 |
| `data/processed/aspect_sentiment.csv` | 方面+极性 |
| `artifacts/metrics/ner_f1.json` | 实体 F1 |
| `pitch_assets/no05_ner/bio_example.png` | BIO 示例 |
| `pitch_assets/no05_ner/aspect_cover.png` | 5 类覆盖 |
| `pitch_assets/no05_ner/ner_f1_card.png` | F1 卡 |

------------------------------------------------------------------------

# 五、对接

| 交谁 | 交什么 |
| ---- | ------ |
| 陈江平 / 属性 BERT | `aspect_sentiment.csv`、`aspect_ner.jsonl` |
| 6号 | NER 路演图 |

------------------------------------------------------------------------

# 六、函数级接口契约

> 文件：`pipelines/aspect_ner/annotate.py`、`train_ner.py`、`predict.py`

```python
def load_annotation_pool(
    path: str = "data/samples/aspect_annotate_pool.csv",
) -> list[dict]:
    """上游: 杨国东 sample_for_annotation()
    返回: [{"id": str, "sentence": str}, ...]
    """

def annotate_bio(sentence: str, aspect_spans: list[tuple[int,int,str]]) -> dict:
    """人工/半自动 BIO 标注辅助。
    入参: sentence；aspect_spans — [(start, end, aspect_type), ...]
    返回: {
        "id": str,
        "tokens": list[str],
        "labels": list[str],     # O, B-质量, I-质量, ...
    }
    """

def export_ner_jsonl(
    records: list[dict],
    path: str = "data/processed/aspect_ner.jsonl",
) -> int:
    """写出 BIO 语料，返回条数"""

def export_aspect_sentiment(
    records: list[dict],
    path: str = "data/processed/aspect_sentiment.csv",
) -> int:
    """方面+极性表。
    每行: { "sentence": str, "aspect": str, "polarity": int, "aspect_span": str? }
    下游: 陈江平 load_aspect_dataset()；属性BERT load_aspect_dataset()
    """

def train_ner(config: dict) -> dict:
    """BertForTokenClassification。
    返回: { "entity_f1": float, "model_path": str }  # f1 ≥ 0.85
    """

def predict_entities(text: str) -> list[dict]:
    """推理：实体 span + 极性（供前端高亮）。
    入参: text — 单条评论
    返回: [{
        "aspect": str,           # 标准属性名，如 "物流"
        "text": str,             # 原文片段
        "start": int, "end": int,
        "polarity": int,         # 0|1
        "polarity_text": str,
    }, ...]
    下游: 郑平高 AnalyzeResult.entities[]（与 aspect/predict 合并或单独字段）
    """
```

### 与郑平高 `AspectEntity` 对齐

| 5号字段 | 郑平高 schema 字段 | 类型 |
| ------- | ------------------ | ---- |
| aspect | aspect | str |
| text | text | str |
| start | start | int |
| end | end | int |
| polarity | polarity | int |
| polarity_text | polarity_text | str |

------------------------------------------------------------------------

# 七、交付自检

| 检查项 | 达标 |
| ------ | ---- |
| BIO + polarity 双标注完成 | ☐ |
| ≥5 类实体 | ☐ |
| 实体 F1≥85% | ☐ |
| 样本已交陈江平 / 属性 BERT | ☐ |
| 路演三图已交 | ☐ |
